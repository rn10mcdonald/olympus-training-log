# Path to the SQLite database file.
# On Railway/Render, point this at a mounted persistent volume, e.g.:
#   DB_PATH=/data/olympus.db
DB_PATH=olympus.db

# Port the server listens on (set automatically by Railway / Render)
PORT=8000
